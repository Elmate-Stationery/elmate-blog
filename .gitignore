node_modules/
.DS_Store

# Build output — regenerated by `npm run build` (and by CI on every deploy).
# blog/index.html is the source template and IS tracked; the per-post pages
# under blog/<slug>/ are generated by scripts/prerender.mjs.
blog/*/
assets/css/tailwind.css
